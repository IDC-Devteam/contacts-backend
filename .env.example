# Supabase setup
SUPABASE_URL=your-supabase-url
SUPABASE_SERVICE_KEY=your-service-role-key
USER_PIN=your-secure-pin

# In dev, all origins are allowed automatically.
# In production, ONLY these origins are allowed (comma-separated).
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:19006,https://contacts.idc.app

# Change to 'production' when deploying live
NODE_ENV=development

# 🛰 IDC LOG — Stardate 4257.2
# First contact: Orion-5 Sector
# Token fragment: Δ9Q
